plugins {
    id 'java'
}

repositories {
    jcenter()
}

dependencies {
	compile([
		'com.netflix.feign:feign-core:8.18.0',
		'com.netflix.feign:feign-gson:8.18.0'
	])
	testCompile([
		'junit:junit:4.12'
	])
}
